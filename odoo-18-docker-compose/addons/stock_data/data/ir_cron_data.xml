<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Removed streaming starter cron -->

    <!-- Auto fetch every minute using wizard (realtime fallback) -->
    <record id="ir_cron_fetch_securities_all" model="ir.cron">
        <field name="name">Fetch All Securities Data (3m)</field>
        <field name="model_id" ref="model_wizard_fetch_market_data"/>
        <field name="state">code</field>
        <field name="code">model.cron_fetch_securities_all()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="active" eval="True"/>
    </record>

    <record id="ir_cron_fetch_index_all" model="ir.cron">
        <field name="name">Fetch All Index Data (3m)</field>
        <field name="model_id" ref="model_wizard_fetch_market_data"/>
        <field name="state">code</field>
        <field name="code">model.cron_fetch_index_all()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="active" eval="True"/>
    </record>

    <record id="ir_cron_fetch_all_ohlc" model="ir.cron">
        <field name="name">Fetch All OHLC Data (1m, cursor)</field>
        <field name="model_id" ref="model_wizard_fetch_market_data"/>
        <field name="state">code</field>
        <field name="code">model.cron_fetch_all_ohlc()</field>
        <field name="interval_number">2</field>
        <field name="interval_type">minutes</field>
        <field name="active" eval="True"/>
    </record>

    <record id="ir_cron_fetch_daily_index" model="ir.cron">
        <field name="name">Fetch Daily Index (3m, cursor)</field>
        <field name="model_id" ref="model_wizard_fetch_market_data"/>
        <field name="state">code</field>
        <field name="code">model.cron_fetch_daily_index()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="active" eval="True"/>
    </record>

    <!-- Removed streaming monitor cron -->

    <!-- Ultra-fast OHLC fetch for realtime data (every minute) -->
    <record id="ir_cron_realtime_ohlc" model="ir.cron">
        <field name="name">Realtime OHLC Data (1m)</field>
        <field name="model_id" ref="model_wizard_fetch_market_data"/>
        <field name="state">code</field>
        <field name="code">model.cron_realtime_ohlc()</field>
        <field name="interval_number">2</field>
        <field name="interval_type">minutes</field>
        <field name="active" eval="True"/>
    </record>

</odoo>
